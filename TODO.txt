Minor
- last round, one card left, auto play everyones hands
- Unit Testing
- Make class variable sets private where possible
- convert 'chosen card' to 'TableCardArea' for all play, should be an array
- Replace linq concats of dealth cards with a refernce list of the original 52 cards
- Replace GameManager's 'while (CurrentState != GameState.EndGame)' with 'public void Tick()'
- Add explicit transition methods for game states
- Add a event for the player to click to steal the trump, then discard

Major
- Add table class
- Event-Driven Design
- Custom Exceptions
- Extend Deck/Score Rules
- Data-driven design: JSON or XML for card definitions
- Configuration file: Define the rules of your card game in a seperate XML or Json file
- Played Card History
- Separation of states: GameState, PlayerState
- Make Hand its own object class
- Create a User object for functions outside of game loop e.g. start new game, exit
- Add CPU AI smart card selection 
- Move stealing to be in the first round of play

Optional
- Ascii art for the trump suit

Bugs
- Ace of Hearts is not renegable if hearts are led but hearts are not trumps and the player has no hearts except for the Ace of hearts

Done
- Circular bufer for players, Input Validation, replaced ElementAt with '[]' operator, Magic Numbers, playerTableArea, Fixed setPlayableCards
- Separate Logic from Presentation, Gamemanager: Divide methods, Move PlayCard methods to Player class, GameManager variables privatized
- stealing
- reneging the ace of hearts when non heart suit is led
- Human player HiddenHand mode, CustomConsole, Dev mode, GameConfig, CardsPlayed Dictionary
- JSON GameConfig Singleton, Newtonsoft.Json, RulesEngine
- Refuse illegal cards played
- Replace GameConfig singleton with Dependency Injection
- RulesEngine
- Define the order of card scores in json rather than int increases
- Remove mutable variables from Card.cs, compute card values and scores when needed with rulesengine.cs
- Remove console prints from Player card methods
- Remove unused library includes

Not Needed

